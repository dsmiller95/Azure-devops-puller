{
  "name": "azure-devops-poller",
  "version": "1.0.0",
  "description": "package to pull data from azure devops",
  "main": "build/index.js",
  "scripts": {
    "build": "npx tsc && npm run zip",
    "zip": "bestzip build.zip build/ node_modules/ package.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dan Miller",
  "license": "ISC",
  "dependencies": {
    "azure-devops-node-api": "^10.0.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.4",
    "@types/node": "^13.7.0",
    "typescript": "^3.7.5",
    "bestzip": "^2.1.5"
  }
}
